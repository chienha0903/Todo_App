apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: users-allow-bff-only
  namespace: todo-app
spec:
  podSelector:
    matchLabels:
      app: users
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: todo-bff
      ports:
        - port: 50052
          protocol: TCP